# Safe client-visible configuration only.
# Values prefixed EXPO_PUBLIC_ are embedded in the mobile bundle and must never contain secrets.

EXPO_PUBLIC_APP_ENV=development
EXPO_PUBLIC_PROCESSING_MODE=local
EXPO_PUBLIC_API_URL=
